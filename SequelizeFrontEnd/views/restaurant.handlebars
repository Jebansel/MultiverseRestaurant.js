<div class="restaurant">
  <h3>{{restaurant.name}}</h3>
  <img src="{{restaurant.imagelink}}" alt="{{restaurant.name}}" />
  <div class="promo-blocks__actions">
    <a href="/restaurants/{{restaurant.id}}/edit" data-id="{{restaurant.id}}" class="anchor--button">
      Edit
    </a>
    <button type="button" data-id="{{restaurant.id}}">
      Delete
    </button>
    <a href="/restaurants/{{restaurant.id}}/add" data-id="{{restaurant.id}}" class="anchor--button">
      Add new menu
    </a>



  </div>
</div>